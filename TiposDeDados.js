//var

var valtura = 5;
var vcomprimento = 7;

varea = valtura * vcomprimento;
console.log(varea);

//let
let lforma = 'retângulo';
let laltura = 5;
let lcomprimento = 7;
let larea;

if (lforma === 'retângulo') {
    larea = laltura * lcomprimento;
} else {
    larea = (laltura * lcomprimento) / 2;
}

console.log(larea);

//const
const cforma = 'quadrado';
const caltura = 5;
const ccomprimento = 7;
let carea;

if (cforma === 'quadrado') {
    carea = caltura * ccomprimento;
} else {
    carea = (caltura * ccomprimento) / 2;
}

console.log(carea);